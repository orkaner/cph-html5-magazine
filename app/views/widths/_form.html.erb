<%= form_for @width, :html=>{:class=>'form-horizontal'}  do |f| %>
    <% if @width.errors.any? %>
    <div class="alert alert-error" id="error_explanation">
      <h2><%= pluralize(@width.errors.count, "error") %> prohibited this width from being saved:</h2>

      <ul>
      <% @width.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="control-group">
      <%= f.label :name,:class=>"control-label" %>
      <div class="controls">
        <%= f.text_field :name %>
      </div>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
