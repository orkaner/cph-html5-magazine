<%= form_for(@container) do |f| %>
  <% if @container.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@container.errors.count, "error") %> prohibited this container from being saved:</h2>

      <ul>
      <% @container.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :grid_id %><br />
      <%= f.select :grid_id, Grid.order("name").collect{|s| [(s.name), s.id]} %>
    </div>

    <div class="field">
      <%= f.label :width_id %><br />
      <%= f.select :width_id, Width.order("name").collect{|s| [(s.name), s.id]} %>
    </div>

  <div class="field">
    <%= f.label :data_sizes %><br />
    <%= f.text_field :data_sizes %>
  </div>

  <div class="field">
    <%= f.check_box :pictures %>
    <%= f.label :pictures, :class => "checkbox inline" %>
  </div>

  <div class="field">
    <%= f.check_box :text %>
    <%= f.label :text, :class => "checkbox inline" %>
  </div>

  <div class="field">
    <%= f.check_box :video %>
    <%= f.label :video, :class => "checkbox inline" %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
