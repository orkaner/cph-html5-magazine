<%= form_for @videolink, :html=>{:class=>'form-horizontal'}  do |f| %>
    <% if @videolink.errors.any? %>
    <div class="alert alert-error" id="error_explanation">
      <h2><%= pluralize(@videolink.errors.count, "error") %> prohibited this videolink from being saved:</h2>

      <ul>
      <% @videolink.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="control-group">
      <%= f.label :title,:class=>"control-label" %>
      <div class="controls">
        <%= f.text_field :title %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :embed_code,:class=>"control-label" %>
      <div class="controls">
        <%= f.text_field :embed_code %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :embedded, :class => "control-label" %>
      <div class="controls">
        <%= f.check_box :embedded, :class => "checkbox inline" %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :article_id,:class=>"control-label" %>
      <div class="controls">
        <%= f.number_field :article_id %>
      </div>
    </div>

   <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
